// Добавляем обработчики событий для нажатия на карточку
const cards = document.querySelectorAll('.card');
cards.forEach(card => {
  card.addEventListener('click', () => {
    // Действие при нажатии на карточку
  })
});
